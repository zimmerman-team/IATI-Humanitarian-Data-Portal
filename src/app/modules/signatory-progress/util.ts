import { LineChartCardModel } from 'app/components/surfaces/Cards/LineChartCard/model';

export function formatLineChart(humPublisherData, publisherData) {
  const data: LineChartCardModel = { values: { values: [] } };
}
